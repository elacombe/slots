#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../web-component-env/src/wce" "$@"
  ret=$?
else 
  node  "$basedir/../web-component-env/src/wce" "$@"
  ret=$?
fi
exit $ret
